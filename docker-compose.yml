name: codex2api

services:
  codex2api:
    image: crpi-dzjyl2rfnlfugj1m.cn-shanghai.personal.cr.aliyuncs.com/frogclaw/codex2api:latest
    container_name: codex2api
    ports:
      - "${BIND_HOST:-0.0.0.0}:${CODEX_PORT:-3200}:${CODEX_PORT:-3200}"
    env_file:
      - .env
    volumes:
      - image-assets:/data
      - ./logs:/app/logs
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:$${CODEX_PORT:-3200}/health >/dev/null || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 60s
    stop_grace_period: 60s
    restart: unless-stopped
    networks:
      - codex2api-net

  postgres:
    image: postgres:18-alpine
    container_name: codex2api-postgres
    environment:
      POSTGRES_USER: ${DATABASE_USER:-codex2api}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-codex2api}
      POSTGRES_DB: ${DATABASE_NAME:-codex2api}
      PGDATA: /var/lib/postgresql/data
      TZ: ${TZ:-Asia/Shanghai}
    volumes:
      - pgdata:/var/lib/postgresql/data
    # ports:
    #   - "5500:5500"   # 取消注释以便本地调试
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USER:-codex2api}"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    networks:
      - codex2api-net

  redis:
    image: redis:7-alpine
    container_name: codex2api-redis
    command: redis-server --appendonly yes
    environment:
      TZ: ${TZ:-Asia/Shanghai}
    volumes:
      - redisdata:/data
    # ports:
    #   - "5501:5501"   # 取消注释以便本地调试
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    networks:
      - codex2api-net

networks:
  codex2api-net:
    driver: bridge

volumes:
  pgdata:
    name: codex2api_pgdata
  redisdata:
    name: codex2api_redisdata
  image-assets:
    name: codex2api_image-assets
